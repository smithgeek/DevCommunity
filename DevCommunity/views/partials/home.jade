.modal.fade(id='AddTopicModal' aria-hidden='true' role='dialog' ng-controller='AddMeetingController as controller')
  .modal-dialog.modal-lg
    .modal-content.highlight
      button.close(type="button" data-dismiss="modal" aria-hidden="true") &times;
      h1 Add Idea
      form(role='form')
        .form-group
          input.form-control(type='text' placeholder='Short Description' ng-model='meeting.description')
        .form-group
          textarea.form-control(type='text' placeholder='Details' ng-model='meeting.details')
        button.btn.btn-lg.btn-success.add-modal-button(type='button' ng-click='controller.AddMeeting()') Submit
        span  
        button.btn.btn-lg.btn-danger.add-modal-button(type='button' data-dismiss='modal') Cancel

.jumbotron
  .row
    h1 Last Meeting
  .row
    if preview
      .col-sm-4(style="min-width: 300px")
        div
          #{preview}
      .col-md-8
        h3 
          a(href='#') #{lastMeetingTitle}
        p
          | #{description}
    else
      p
        | #{description}
div(ng-controller='HomeController as controller')
  .page-header
    h1 Meeting Ideas  
      button.btn.btn-info.add-button(type='button' ng-click='controller.AddTopic()')
        span.glyphicon.glyphicon-plus
        span   Add

  div(ng-repeat='meeting in meetings')
    .media
      .pull-left
        button.btn.btn-success.vote-button(class='vote-btn-{{meeting._id}}' type='button' ng-click='meeting.Vote()' ng-hide="meeting.HasUserVoted()")
          span.glyphicon.glyphicon-arrow-up
          span  Vote
        button.btn.btn-danger.vote-button(class='vote-btn-{{meeting._id}}' type='button' ng-click='meeting.RemoveVote()' ng-show="meeting.HasUserVoted()")
          span.glyphicon.glyphicon-remove-sign
          span  Remove
        .vote
          div.vote-count {{meeting.vote_count}}
          //span.glyphicon.glyphicon-ok-sign
          span.vote-description  votes

      .media-body
        .panel.panel-primary
          .panel-heading
            //.pull-right
              button.editable
                span.glyphicon.glyphicon-edit
              button.editable
                span.glyphicon.glyphicon-trash
            h3.panel-title {{meeting.description}}
          .panel-body
            div(ng-bind-html="meeting.details")